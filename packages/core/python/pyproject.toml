[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "caracalai-core"
version = "0.1.0"
description = "Caracal core utilities shared across Python packages: scope evaluation and primitives"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["caracalai_core"]
